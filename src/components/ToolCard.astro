---
// File: src/components/ToolCard.astro
interface Props {
  name: string;
}
const { name } = Astro.props;
---
<div 
  class="bg-primary rounded-lg p-4 flex flex-col items-center justify-center text-center gap-3 h-28 
         hover:bg-gray-800 
         transition-all duration-300 transform hover:-translate-y-2 hover:scale-105"
>
  <slot />
  <p class="text-text-secondary text-sm font-semibold">{name}</p>
</div>